sudo: required
dist: bionic
os: linux
language: node_js
node_js: 10.14.1
services:
  - docker

before_script:
  - cd socket-service
  - chmod +x ./tools/scripts/build/build.sh
  - npm install
  - npm run build:ts

jobs:
  include:
    - stage: unit test
      script:
        - npm run test:unit

